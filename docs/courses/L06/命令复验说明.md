# L06 命令复验说明

> 历史／选读资料：商品导入、六项检查及七行实验属于旧路线。当前必做库存主线及命令见[辅导资料](辅导资料.md)和[实践操作手册](实践操作手册.md)；下文旧记录不作为当前接入或验收结论。

复验日期：2026-09-09。以下是作者在 Windows、项目 .venv、独立运行目录中的实际检查，不是你的学习记录或人审接受。

## 实际交付

- L05 来源任务：TASK-CC59509BA9；三份原检查按冻结哈希核对后复用。
- L06 成功任务：TASK-2398C4CDF7；状态 `review`，implementation_evidence 为 true。
- 起点：5 项中 2 通过、3 阻断；终点：5 项全部通过。
- 实际修改：eval/l06_runner.py 与 flowerp/service.py；六份冻结检查未变。
- 新模块独立合同：9 项通过；其实际聚合的库存与 L05 三项检查通过。
- 换输入：11−4=7，查询与 CSV 一致；再次申请 8 被拒绝，库存行与事件不变。
- 八类参考分支退出码：0、1、0、1、2、2、2、1，与手册一致。
- 故意矛盾的报告及拿旧报告顶替本次的请求均被消费者拒绝。

## 保留的失败与边界

首次准备发现历史标签缺少 CSV 服务方法，脚本在写入前停止；已补充来源可追溯的前置恢复，明确不计个人增量。首次交付 TASK-230F2BA734 虽完成实现，后置合同遇到临时文件 PermissionError，因此保留 rework；未删除该失败，也未用独立重跑伪装原任务成功。随后从同一红色提交重新创建上面的成功任务。

本机 Codex 执行采用仅本次进程的 WORKBENCH_CODEX_CONFIG_MODE=isolated；全局设置未改。验证使用独立 verification-runtime，未代替既有 L04 工作台的人审。命令复验没有证明客户 Web 全链路、并发库存场景、任意项目适配、逐项超时隔离或真实教学达成。

## 原始记录

本机运行目录：`D:/work/CodexFDE/.runtime/l06-practice/20260909-162444-d726d379`。其中 session.json 保存环境，preparation.json 保存来源与教学缺陷，frozen-checks.json 保存检查哈希，submission.json 保存最终交付，evidence/ 保存原始命令记录。失败记录与成功记录一起保留，不能只选绿色摘要。

本轮累计原始命令记录 30 份（含预期失败及排错记录）；具体证据以该目录为准，不把记录数当作测试用例数。正式验收仍需真实复验者与审核者的判断。

## 补充验证

- 手册 12 个 PowerShell 代码块语法检查通过；实际执行过程另有原始记录，不以语法检查替代实跑。
- 工作台启动入口在独立运行目录、临时空闲端口 60381 返回 HTTP 200，首页包含“事项与决策”。仅验证入口启动，不代表浏览器全流程或人审操作。
- 控制仓库全量阻断 Eval 首次 28/29，因临时 diff.txt 读取 PermissionError 阻断；保留 control-blocking.json 后原命令换新报告复验 29/29 通过，未修改断言。该结果与本讲候选五项检查分别记录。
