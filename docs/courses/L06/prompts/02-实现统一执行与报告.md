# 分开修复汇总与库存导出

候选路径由我提供。允许文件仅 eval/l06_runner.py 和 flowerp/service.py，保留原检查、基线及历史报告。

先只修汇总器，按实际分项计算阻断与观察失败，保留原因、耗时和选择校验。运行九项合同，并确认查询与导出不一致仍让业务 block。

再修 service.py 的 CSV 可用字段，遵守在库减预占，保留查询、其他字段及超额拒绝行为。原检查复验后，换在库 13、预占 4，确认可用 9、请求 10 拒绝且状态不变。说明实际差异与未覆盖边界。
