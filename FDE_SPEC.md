# FDE_SPEC｜电商订单缺货时阻止超卖并生成补货建议

## 来源

课程主线需求 `REQ-ECOM-001`，由“活动商品发生库存争抢时，需要阻止超卖并可追踪地补货”整理而来。

## 目标

创建消费者订单并原子预占库存；并发到达的后续订单库存不足时返回明确缺口，不允许部分预占或超卖，并可据此生成补货建议。采购建议必须经电商运营人工批准，批准后才能用幂等键执行入库并恢复订单履约。

渠道接单、回传租约和资金对账可以存在于终态电商产品中，但必须作为工作台交付物留下需求编号、Eval 和人审；不能只作为菜单存在。

## 非目标

- 不把渠道、履约回传、三单匹配或总账页面当作尚未经工作台交付的学员本人成果。
- 不自动批准采购，不接入真实供应商系统。
- 不允许模型绕过库存和审批规则直接写数据库。

终态产品可以包含渠道订单、回传租约、三单匹配与资金对账；它们必须能回指一次 Spec → Eval → 人审。答不出交付因果的，只算产品挑战，须经 L15 用工作台再交一次才计入学员成果。索引见 `workbench/product_lineage.py`。

## 约束

- 可用库存不得为负。
- 同一入库幂等键只能生效一次。
- 订单和采购状态只能走合法迁移。
- 所有验收通过 `python -X utf8 -m eval.harness --suite blocking` 复验。

## 验收用例

1. 库存 5 件时预占 3 件成功，可用库存为 2。
2. 再预占 3 件失败，并报告 SKU 与缺口；库存不变。
3. 取消已预占订单后释放 3 件库存。
4. 未审批采购建议不得入库。
5. 已审批采购用相同幂等键执行两次，库存只增加一次。
6. 门面、库存导出与库存页对同一入库给出同一个 `available`。
7. 渠道/资金能力要么能指出工作台证据链，要么标明产品挑战。

## 完成定义

代码、测试、阻断级 Eval、运行摘要全部通过；仓库内无密钥和运行数据库。
